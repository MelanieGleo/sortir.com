<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>{% block title %}Sortir.com{% endblock %}</title>

    {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('css/style.css') }}">
        <link rel="icon" href="{{ asset('images/favicon.ico') }}">
        <link rel="preconnect" href="https://fonts.gstatic.com">
        <link href="https://fonts.googleapis.com/css2?family=Akaya+Telivigala&display=swap" rel="stylesheet">
    {% endblock %}

    {% block css %}{% endblock %}

</head>

<body>
{% block bandeau %}
    <header class="bandeau">
        <div class="element col-9x12">
            <h1 class="mainTitle"><a href="{{ path ('app_sortie') }}">Sortir.com</a></h1>
        </div>

        <div class="element col-3x12">
            {% block lien %}
                {% if app.user %}

                    {% if app.user.administrateur %}
                        <a href="{{ path('app_register') }}">Inscrire un participant</a>
                    {% endif %}

                    {% block autreLienProfil %}
                        <a href="{{ path('app_profil') }}"> Mon profil </a>
                    {%endblock%}
                    <a href="{{ path ('app_ajoutSortie') }}">Ajouter une Sortie</a>
                    <a href="{{ path('app_logout') }}"> Se déconnecter </a>
                    <span>Participant: {{ app.user.prenom|first|upper }}{{ app.user.prenom|slice(1)|lower }} {{ app.user.nom|first|upper  }} . </span>
                    <span>Date du jour: {{ "now"|date("m/d/Y") }}</span>

                {% else %}

                    <a href=" {{ path ('app_login') }}">Connexion</a>

                {% endif %}
            {% endblock %}
        </div>
    </header>
{% endblock %}

{% block body %}{% endblock %}

{% block footer %}{% endblock %}

{% block javascripts %}{% endblock %}
</body>


</html>
